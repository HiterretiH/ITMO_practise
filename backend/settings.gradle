plugins {
    // Скачивает JDK под toolchain (см. build.gradle java { toolchain 21 }), если в системе нет Java 21
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.10.0'
}

rootProject.name = 'backend'
